# Shared PostgreSQL (same as Golden Years Club — read-only access)
DATABASE_URL=postgresql://user:pass@host:5432/golden_years

# Canonical site URL
NEXT_PUBLIC_SITE_URL=https://littlebuddy.club

# Environment identifier (staging shows a banner)
# ENVIRONMENT=production
